public class Enums {
    public enum Condition { POOR, FAIR, GOOD, EXCELLENT }
    public enum RestorationStatus { NOT_STARTED, IN_PROGRESS, COMPLETED }
    public enum ProjectStatus { NOT_STARTED, IN_PROGRESS, COMPLETED }
    public enum ComponentType { BODYWORK, PAINTWORK, ENGINE, DRIVETRAIN, SUSPENSION, INTERIOR }
    public enum ComponentStatus { NOT_STARTED, IN_PROGRESS, COMPLETED }
    public enum TaskStatus { NOT_STARTED, IN_PROGRESS, COMPLETED }
}